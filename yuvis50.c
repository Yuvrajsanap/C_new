#include <stdio.h>
int main()
{
    int cars,i=0;
    char a,b;
    char *ptr;
    
while (i<3)
{
    printf("the cars is \n");
    scanf("%d",&cars);
    printf("the value of a is \n");
    scanf("%c",&a);
    printf("the value of b is \n");
    scanf("%c",&b);
    // ptr = (char *) malloc((cars+1)*sizeof(char));
    // printf(" the value pointer \n");
    // scanf("%s",&ptr);
    // printf("the value is %s\n",ptr);
    // free(ptr);
    // i=i+1;
}

    return 0;
}